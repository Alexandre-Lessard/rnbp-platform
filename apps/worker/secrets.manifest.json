{
  "$comment": [
    "Secrets the Worker must have before a deploy is allowed, checked by",
    "ops/check-secrets.mjs in CD. Most of these are `.optional()` in the Zod",
    "schema so the Worker still boots without them — it just silently loses a",
    "feature (no emails, shop returns 503). That silence is what this manifest",
    "exists to prevent.",
    "",
    "`required` must match the keys the schema refuses to default; a unit test",
    "fails if the two drift apart. The per-environment lists are a judgement",
    "call about what that environment cannot be useful without."
  ],

  "required": ["JWT_PRIVATE_KEY", "JWT_PUBLIC_KEY", "PASSWORD_PEPPER"],

  "environments": {
    "production": {
      "expected": [
        "BREVO_API_KEY",
        "STRIPE_SECRET_KEY",
        "STRIPE_WEBHOOK_SECRET",
        "R2_PUBLIC_URL",
        "META_CAPI_TOKEN",
        "GOOGLE_CLIENT_ID",
        "GOOGLE_CLIENT_SECRET"
      ]
    },
    "staging": {
      "expected": ["R2_PUBLIC_URL"],
      "notes": {
        "BREVO_API_KEY": "Not expected: staging logs emails to the console instead of sending them.",
        "STRIPE_SECRET_KEY": "Not expected yet — add here once staging has test keys wired up.",
        "META_CAPI_TOKEN": "Not expected: staging must not report conversions to the live Meta dataset.",
        "GOOGLE_CLIENT_ID": "Not expected: the Google OAuth client only whitelists badgeid.ca and localhost, so staging has no Google sign-in to configure.",
        "GOOGLE_CLIENT_SECRET": "Not expected: same reason as GOOGLE_CLIENT_ID."
      }
    }
  }
}
